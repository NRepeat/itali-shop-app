---
phase: 01-sync-foundation-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/service/sync/base.syncer.ts
  - prisma/schema.prisma
  - app/service/sync/audit.service.ts
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - "A standardized BaseSyncer interface exists for future entity sync logic."
    - "All sync attempts and outcomes are recorded in a centralized audit log."
  artifacts:
    - path: "app/service/sync/base.syncer.ts"
      provides: "Abstract base class for sync operations"
      min_lines: 30
      contains: "abstract class BaseSyncer"
    - path: "prisma/schema.prisma"
      provides: "SyncAuditLog model"
      contains: "model SyncAuditLog"
    - path: "app/service/sync/audit.service.ts"
      provides: "Utility for logging to SyncAuditLog"
      min_lines: 10
  key_links:
    - from: "any syncer implementation"
      to: "SyncAuditLog"
      via: "audit.service.ts"
      pattern: "auditService\.log"
---

<objective>
Implement the foundational abstract `BaseSyncer` class and establish a centralized audit log for all synchronization operations.

Purpose: This plan establishes the core pattern for all future entity synchronization and provides the necessary visibility into sync attempts and outcomes, fulfilling the CORE-01 requirement.
Output: `app/service/sync/base.syncer.ts`, updated `prisma/schema.prisma` with `SyncAuditLog` model, and `app/service/sync/audit.service.ts`.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sync-foundation-architecture/01-CONTEXT.md
@.planning/phases/01-sync-foundation-architecture/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Implement BaseSyncer Abstract Class</name>
  <files>app/service/sync/base.syncer.ts</files>
  <action>
    Create `app/service/sync/base.syncer.ts`.
    Define an abstract `BaseSyncer` class that includes:
    - An abstract `queueName` property (string).
    - An `handleWebhook(payload: TPayload)` method that includes `isStale` check.
    - An abstract `process(payload: TPayload)` method.
    - An abstract `reconcile()` method.
    - A protected abstract `isStale(timestamp: string)` method to compare `updated_at` timestamps to prevent race conditions (as discussed in RESEARCH.md pitfalls).
    - Use TypeScript generics for `TPayload`.
    - Ensure the class is `export abstract`.
  </action>
  <verify>
    The file `app/service/sync/base.syncer.ts` exists and contains an `abstract class BaseSyncer` with the specified abstract methods and properties.
    Check for proper TypeScript syntax and generic usage.
  </verify>
  <done>
    The `BaseSyncer` abstract class is defined and ready for concrete implementations in subsequent phases.
  </done>
</task>

<task type="auto">
  <name>Implement Centralized Sync Audit Log</name>
  <files>
    prisma/schema.prisma
    app/service/sync/audit.service.ts
  </files>
  <action>
    1. **Update `prisma/schema.prisma`**:
       - Add a new model `SyncAuditLog` with the following fields:
         - `id`: String, @id, @default(cuid())
         - `entityType`: String (e.g., "PRODUCT", "ORDER", "CUSTOMER")
         - `entityId`: String (Shopify ID of the entity)
         - `status`: String (e.g., "SUCCESS", "FAILURE", "QUEUED")
         - `message`: String? (Optional detailed message)
         - `error`: Json? (Optional JSON field for error details)
         - `createdAt`: DateTime, @default(now())
       - Ensure `SyncAuditLog` is part of the `PrismaClient` generation.
    2. **Run Prisma Migration**:
       - Execute `prisma db push` to apply the schema changes to the database.
       - *Note: No new migration file should be created at this stage, `db push` is sufficient for initial setup.*
    3. **Create `app/service/sync/audit.service.ts`**:
       - Implement a utility class or functions (`AuditService`) to interact with the `SyncAuditLog` model via Prisma.
       - Include methods like `log(entityType, entityId, status, message?, error?)` for recording sync events.
       - Inject or import the Prisma client instance.
  </action>
  <verify>
    <automated>
      grep -q "model SyncAuditLog" prisma/schema.prisma && 
      npx prisma db push --skip-generate && 
      test -f app/service/sync/audit.service.ts && 
      grep -q "class AuditService" app/service/sync/audit.service.ts
    </automated>
    <manual>
      Verify that `prisma/schema.prisma` contains the `SyncAuditLog` model.
      Check the database (if accessible) to ensure the `SyncAuditLog` table was created.
      Confirm `app/service/sync/audit.service.ts` exists and has logging methods.
    </manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    A dedicated `SyncAuditLog` table exists in the database, and a service for centralized logging of sync events is available.
  </done>
</task>

</tasks>

<verification>
After both tasks in this plan are complete:
- The `BaseSyncer` abstract class should be well-defined, providing a clear contract for future sync implementations.
- The `SyncAuditLog` table should be present in the database, and the `AuditService` should be able to write entries to it.
</verification>

<success_criteria>
- `app/service/sync/base.syncer.ts` exists and defines the `BaseSyncer` abstract class with expected methods.
- `prisma/schema.prisma` contains the `SyncAuditLog` model.
- `app/service/sync/audit.service.ts` exists and provides an interface for logging sync events.
- `prisma db push` completes successfully, creating the `SyncAuditLog` table.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sync-foundation-architecture/01-01-SUMMARY.md`
</output>
