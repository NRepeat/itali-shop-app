---
phase: 01-sync-foundation-architecture
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - app/routes/webhooks.orders.cancelled.tsx
  - app/routes/webhooks.orders.create.tsx
  - app/routes/webhooks.orders.fulfilled.tsx
  - app/routes/webhooks.orders.paid.tsx
  - app/routes/webhooks.products.update.tsx
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - "All webhook handlers are refactored to use the BullMQ queuing system."
    - "Webhook handlers no longer contain any business logic, only queueing logic."
    - "The correct queue is selected based on the webhook topic."
  artifacts:
    - path: "app/routes/webhooks.products.update.tsx"
      provides: "Refactored webhook handler for product updates"
      min_lines: 10
      contains: "getSyncQueue"
---

<objective>
Verify that all relevant webhook handlers have been refactored to push jobs to the appropriate BullMQ queues, and create a summary for this work.

Purpose: This plan finalizes the webhook-to-queue workflow by ensuring all webhook endpoints are consistently using the new asynchronous processing system.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sync-foundation-architecture/01-CONTEXT.md
@app/service/sync/sync.registry.ts
</context>

<tasks>
<task type="auto">
  <name>Verify Webhook Handler Refactoring</name>
  <files>
    - app/routes/webhooks.orders.cancelled.tsx
    - app/routes/webhooks.orders.create.tsx
    - app/routes/webhooks.orders.fulfilled.tsx
    - app/routes/webhooks.orders.paid.tsx
    - app/routes/webhooks.products.update.tsx
  </files>
  <action>
    1. **Verify each webhook handler**:
       - Ensure each file imports `getSyncQueue` from `~/service/sync/sync.registry`.
       - Confirm that each file's `action` function calls `getSyncQueue` with the webhook `topic`.
       - Verify that the `action` function adds a job to the returned queue.
       - Check that there is no other business logic in the webhook handler.
  </action>
  <verify>
    <automated>
      for file in app/routes/webhooks.orders.cancelled.tsx app/routes/webhooks.orders.create.tsx app/routes/webhooks.orders.fulfilled.tsx app/routes/webhooks.orders.paid.tsx app/routes/webhooks.products.update.tsx; do
        grep -q "import { getSyncQueue } from " "$file" &&
        grep -q "const queue = getSyncQueue(topic);" "$file" &&
        grep -q "await queue.add(topic, { shop, topic, payload });" "$file" || exit 1
      done
    </automated>
    <manual>
      Check each of the webhook handlers to ensure they are clean and only contain the logic for adding a job to the queue.
    </manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    All webhook handlers are verified to be using the BullMQ queuing system correctly.
  </done>
</task>
</tasks>

<verification>
After the task in this plan is complete:
- All relevant webhook handlers are verified to be using the new queuing system.
</verification>

<success_criteria>
- All webhook handlers are refactored to use the BullMQ queuing system.
- The `01-03-SUMMARY.md` file is created.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sync-foundation-architecture/01-03-SUMMARY.md`
</output>
