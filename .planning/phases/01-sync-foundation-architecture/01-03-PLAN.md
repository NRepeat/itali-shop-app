---
phase: 01-sync-foundation-architecture
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - app/routes/webhooks.collections.create.tsx
  - app/routes/webhooks.collections.delete.tsx
  - app/routes/webhooks.collections.update.tsx
  - app/routes/webhooks.orders.cancelled.tsx
  - app/routes/webhooks.orders.create.tsx
  - app/routes/webhooks.orders.fulfilled.tsx
  - app/routes/webhooks.orders.paid.tsx
  - app/routes/webhooks.products.update.tsx
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - "Shopify webhooks for collections, orders, and products are successfully received and offloaded to BullMQ queues."
    - "Webhook handlers return an immediate 200 OK response after enqueueing."
    - "Direct processing logic within the original webhook handlers is replaced by queueing mechanisms."
  artifacts: [] # Modifications to existing files, no new artifacts directly created.
  key_links:
    - from: "app/routes/webhooks.collections.create.tsx"
      to: "app/service/sync/sync.registry.ts"
      via: "getSyncQueue().add()"
      pattern: "getSyncQueue\\('collections.*'\\)\\.add"
    - from: "app/routes/webhooks.orders.create.tsx"
      to: "app/service/sync/sync.registry.ts"
      via: "getSyncQueue().add()"
      pattern: "getSyncQueue\\('orders.*'\\)\\.add"
    - from: "app/routes/webhooks.products.update.tsx"
      to: "app/service/sync/sync.registry.ts"
      via: "getSyncQueue().add()"
      pattern: "getSyncQueue\\('products.*'\\)\\.add"
---

<objective>
Refactor existing Shopify webhook handlers for products, orders, and collections to offload processing to the new BullMQ entity-specific queues.

Purpose: This plan integrates the established BullMQ queuing infrastructure by modifying existing webhook handlers to delegate their processing to asynchronous jobs, thereby improving responsiveness and reliability, fulfilling part of the CORE-02 requirement.
Output: Modified existing webhook files that enqueue jobs to BullMQ.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sync-foundation-architecture/01-CONTEXT.md
@.planning/phases/01-sync-foundation-architecture/01-RESEARCH.md
@app/service/sync/sync.registry.ts
</context>

<tasks>

<task type="auto">
  <name>Refactor Webhook Handlers to Enqueue Sync Jobs</name>
  <files>
    app/routes/webhooks.collections.create.tsx
    app/routes/webhooks.collections.delete.tsx
    app/routes/webhooks.collections.update.tsx
    app/routes/webhooks.orders.cancelled.tsx
    app/routes/webhooks.orders.create.tsx
    app/routes/webhooks.orders.fulfilled.tsx
    app/routes/webhooks.orders.paid.tsx
    app/routes/webhooks.products.update.tsx
  </files>
  <action>
    For each listed webhook handler file:
    1.  **Import `getSyncQueue`**: Add an import statement for `getSyncQueue` from `app/service/sync/sync.registry.ts`.
    2.  **Extract Topic and Payload**: Identify how the webhook topic and payload are extracted from the request context (e.g., using `request.json()` and `shopify.authenticate.webhook`).
    3.  **Enqueue Job**: Call `getSyncQueue(topic).add(topic, payload)` to add the webhook data as a job to the appropriate BullMQ queue. The job name can simply be the topic name.
    4.  **Remove Direct Processing**: Comment out or remove any direct synchronous processing logic that was previously handled by the webhook. The entire processing should now be delegated to the queue worker.
    5.  **Return 200 OK**: Ensure the webhook handler returns a `new Response(null, { status: 200 })` immediately after enqueueing the job.
  </action>
  <verify>
    <automated>
      grep -q "import { getSyncQueue } from '~/service/sync/sync.registry';" app/routes/webhooks.collections.create.tsx &&
      grep -q "getSyncQueue(" app/routes/webhooks.collections.create.tsx &&
      grep -q "return new Response(null, { status: 200 });" app/routes/webhooks.collections.create.tsx &&
      grep -q "import { getSyncQueue } from '~/service/sync/sync.registry';" app/routes/webhooks.products.update.tsx &&
      grep -q "getSyncQueue(" app/routes/webhooks.products.update.tsx &&
      grep -q "return new Response(null, { status: 200 });" app/routes/webhooks.products.update.tsx
      # Extend these grep checks for other webhook files as needed, or create a simple script to verify all listed files.
    </automated>
    <manual>
      Manually inspect a few webhook files (e.g., `webhooks.products.update.tsx`, `webhooks.orders.create.tsx`) to confirm they import `getSyncQueue`, enqueue jobs, and return a 200 OK. Verify that previous processing logic is removed.
    </manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    All specified webhook handlers for collections, orders, and products are refactored to asynchronously enqueue their payloads to the correct BullMQ queues and return a 200 OK response.
  </done>
</task>

</tasks>

<verification>
After this plan is complete:
- All listed webhook handlers (for collections, orders, products) should import `getSyncQueue` from `sync.registry.ts`.
- Each handler should call `getSyncQueue(...).add(...)` with the appropriate topic and payload.
- Each handler should return a 200 OK response, indicating successful enqueueing, not processing.
</verification>

<success_criteria>
- The `files_modified` webhook files are updated to use the BullMQ queuing mechanism.
- Webhook endpoints are responsive and offload heavy processing to the background worker.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sync-foundation-architecture/01-03-SUMMARY.md`
</output>
