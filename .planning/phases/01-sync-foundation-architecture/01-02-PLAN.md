---
phase: 01-sync-foundation-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/service/sync/queues.ts
  - app/service/sync/sync.registry.ts
  - app/webhook.worker.ts
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - "BullMQ queues are successfully instantiated for product, order, and customer synchronization."
    - "A mechanism exists to map incoming webhook topics to the correct BullMQ queue."
    - "A centralized worker is active and listening to all entity-specific sync queues."
    - "The worker logs job processing status using the AuditService."
  artifacts:
    - path: "app/service/sync/queues.ts"
      provides: "BullMQ queue instances for entities (products, orders, customers)"
      min_lines: 10
      contains: "new Queue('productSyncQueue')"
    - path: "app/service/sync/sync.registry.ts"
      provides: "Function to retrieve queues based on webhook topic"
      min_lines: 5
      exports: ["getSyncQueue"]
    - path: "app/webhook.worker.ts"
      provides: "BullMQ worker processing sync jobs from multiple queues"
      min_lines: 20
      contains: "new Worker("
  key_links:
    - from: "app/service/sync/sync.registry.ts"
      to: "app/service/sync/queues.ts"
      via: "importing queue instances"
      pattern: "import { .* } from './queues';"
    - from: "app/webhook.worker.ts"
      to: "app/service/sync/queues.ts"
      via: "listening to multiple queues"
      pattern: "new Worker\\(\\[.*?\\]"
    - from: "app/webhook.worker.ts"
      to: "app/service/sync/audit.service.ts"
      via: "logging job status"
      pattern: "auditService\\.log"
---

<objective>
Configure BullMQ queues for entity-specific synchronization, establish a sync registry, and create a centralized BullMQ worker to process these jobs.

Purpose: This plan establishes the crucial BullMQ queue and worker infrastructure for reliable asynchronous processing, fulfilling part of the CORE-02 requirement by providing the mechanisms for decoupling webhook reception from processing.
Output: `app/service/sync/queues.ts`, `app/service/sync/sync.registry.ts`, and `app/webhook.worker.ts`.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sync-foundation-architecture/01-CONTEXT.md
@.planning/phases/01-sync-foundation-architecture/01-RESEARCH.md
@app/shopify.server.ts
@app/worker.ts
@app/service/sync/audit.service.ts
</context>

<tasks>
<task type="auto">
  <name>Create Centralized Webhook Worker</name>
  <files>app/webhook.worker.ts</files>
  <action>
    1. **Create `app/webhook.worker.ts`**:
       - Implement a BullMQ `Worker` instance that listens to all entity-specific queues defined in `app/service/sync/queues.ts`. Import these queues.
       - The worker's `process` function should take a job, identify the webhook topic and payload (`job.name` and `job.data`), and perform preliminary logging.
       - For this phase, the worker will primarily log the job details and integrate with the `AuditService` (from Plan 01-01) to log job status (e.g., QUEUED, PROCESSING, SUCCESS, FAILURE). Actual dispatch to `BaseSyncer` implementations will happen in later phases.
       - Configure global worker options (e.g., `concurrency` based on Claude's discretion) and proper error handling for job failures.
  </action>
  <verify>
    <automated>
      test -f app/webhook.worker.ts &&
      grep -q "new Worker" app/webhook.worker.ts &&
      grep -q "import { AuditService } from '../service/sync/audit.service';" app/webhook.worker.ts &&
      grep -q "auditService.log" app/webhook.worker.ts &&
      grep -q "job.name" app/webhook.worker.ts
    </automated>
    <manual>
      Check `app/webhook.worker.ts` for a BullMQ worker setup that listens to multiple queues, includes basic job processing, and integrates with the `AuditService` for logging.
    </manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    A centralized BullMQ worker is implemented, listening to all entity-specific sync queues, and is ready to dispatch jobs and log their status using the AuditService.
  </done>
</task>

</tasks>

<verification>
After both tasks in this plan are complete:
- The BullMQ queues are instantiated and exportable from `queues.ts`.
- The `sync.registry.ts` correctly maps webhook topics to these queues.
- The `app/webhook.worker.ts` is running, listening to these queues, and logs job events via the `AuditService`.
</verification>

<success_criteria>
- `app/service/sync/queues.ts` exists and exports configured BullMQ queues for products, orders, and customers.
- `app/service/sync/sync.registry.ts` exists and correctly maps webhook topics to the respective queues.
- `app/webhook.worker.ts` exists, listens to the sync queues, and logs job processing attempts using `AuditService`.
- The core BullMQ queuing and worker infrastructure is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sync-foundation-architecture/01-02-SUMMARY.md`
</output>
