---
phase: 05-email-improvements-and-frontend-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/email/templates/esputnik/01-zamovlennya-oformleno.html
  - .planning/email/templates/esputnik/02-pidtverdzheno.html
  - .planning/email/templates/esputnik/03-vidpravleno.html
  - .planning/email/templates/esputnik/04-vykonano.html
autonomous: false
requirements:
  - ORD-01
must_haves:
  truths:
    - "All four email templates show body/heading text in near-black (#1a1a1a) not navy (#05125C)"
    - "Prices display as whole hryvnias (e.g. '1250 грн') — no decimal, 'UAH' replaced with 'грн'"
    - "Order date includes time (dd.MM.yyyy HH:mm)"
    - "Logo in header is wrapped in clickable <a href='https://miomio.com.ua'> anchor"
    - "Delivery cost row is absent from Підсумок section in all four templates"
    - "Template 01 shows 'Наші менеджери...' text directly under the h2 heading (not at bottom)"
    - "Template 01 order info line includes creation time"
    - "eSputnik sender display name is set to 'Міо Міо' in account settings"
  artifacts:
    - path: ".planning/email/templates/esputnik/01-zamovlennya-oformleno.html"
      provides: "INITIALIZED email template"
      contains: "#1a1a1a"
    - path: ".planning/email/templates/esputnik/02-pidtverdzheno.html"
      provides: "CONFIRMED email template"
      contains: "грн"
    - path: ".planning/email/templates/esputnik/03-vidpravleno.html"
      provides: "IN_PROGRESS email template"
      contains: "intValue"
    - path: ".planning/email/templates/esputnik/04-vykonano.html"
      provides: "DELIVERED email template"
      contains: "miomio.com.ua"
  key_links:
    - from: "price rendering in each template"
      to: "Velocity intValue() call"
      via: "#set($intCost = $cost.intValue())"
      pattern: "intValue"
    - from: "logo <img> tag"
      to: "clickable anchor"
      via: "<a href=\"https://miomio.com.ua\" target=\"_blank\">"
      pattern: "href.*miomio.com.ua"
---

<objective>
Apply visual and formatting fixes to email templates 01–04 (Замовлення оформлено, Підтверджено, Відправлено, Виконано). After templates are updated, prompt the human to update the sender display name in eSputnik account settings.

Purpose: Correct the color scheme, price format, date format, logo link, and delivery cost row that are wrong in all templates, plus template 01-specific structural change (move manager notice to top). The sender display name "Міо Міо" is an account-level setting — Claude cannot change it, so a checkpoint ensures it is not forgotten.
Output: Four updated HTML files ready to paste into eSputnik editor. Sender display name updated in eSputnik admin.
</objective>

<execution_context>
@/Users/mnmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mnmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-email-improvements-and-frontend-fixes/05-CONTEXT.md
@.planning/phases/05-email-improvements-and-frontend-fixes/05-RESEARCH.md

<interfaces>
<!-- Velocity syntax reference for the changes in this plan. -->

<!-- 1. Price format — strip kopecks, replace UAH with грн -->
<!-- Current: $!data.get('totalCost') $!data.get('currency') -->
<!-- Fix: -->
#set($cost = $!data.get('totalCost'))
#set($intCost = $cost.intValue())
$intCost грн
<!-- Same pattern for $!item.get('cost') $!data.get('currency') inside items loop -->
<!-- And for discount row: -$!data.get('discount') $!data.get('currency') -->

<!-- 2. Date format — add time -->
<!-- Current: $date.format('dd.MM.yyyy', $date.toDate($data.get('date'))) -->
<!-- Fix: -->
$date.format('dd.MM.yyyy HH:mm', $date.toDate($data.get('date')))

<!-- 3. Logo link -->
<!-- Current: <img src="..." alt="Mio Mio" height="55" ...> -->
<!-- Fix: wrap in anchor -->
<a href="https://miomio.com.ua" target="_blank" style="display:block;line-height:0">
  <img src="..." alt="Mio Mio" height="55" style="display:block;margin:0 auto;max-width:180px">
</a>

<!-- 4. Color replacements -->
<!-- Non-status text headings: color:#05125C → color:#1a1a1a -->
<!-- Status badge: KEEP color:#05125C (there is no status badge in templates 01-04, but keep the HEADER background as #05125C) -->
<!-- Specifically: h2, h3 headings and customer name p that use color:#05125C → change to color:#1a1a1a -->
<!-- Item name links: color:#05125C → color:#1a1a1a -->
<!-- Item price: color:#05125C → color:#1a1a1a -->
<!-- "Всього:" and total amount: color:#05125C → color:#1a1a1a -->
<!-- Header background: KEEP background-color:#05125C -->
<!-- Footer background: KEEP background-color:#05125C -->

<!-- 5. Remove delivery cost row -->
<!-- Delete this entire <tr> block (the one with "Доставка:" label): -->
<!--
#if($data.get('shipping') and $data.get('shipping')!= "")
<tr>
  <td style="padding:4px 0;color:#666666">Доставка:</td>
  <td style="padding:4px 0;text-align:right">$!data.get('shipping') $!data.get('currency')</td>
</tr>
#end
-->

<!-- 6. Template 01 specific: move manager notice -->
<!-- Current location: just before FOOTER (<!-- MANAGER NOTICE --> section) -->
<!-- Move to: directly after the <h2>Дякуємо за замовлення!</h2>, before the order number line -->
<!-- New position in TITLE section: -->
<h2 style="color:#05125C;margin:0 0 8px;font-size:24px">Дякуємо за замовлення!</h2>
<p style="margin:0 0 12px;font-size:14px;color:#666666">Наші менеджери зв'яжуться з вами найближчим часом для підтвердження замовлення</p>
<p style="margin:0;font-size:14px;color:#666666">
  Замовлення <strong>$!data.get('externalOrderId')</strong> від $date.format('dd.MM.yyyy HH:mm', $date.toDate($data.get('date')))
</p>
<!-- Remove the old MANAGER NOTICE <tr> block from the bottom -->

<!-- 7. Template 01 duplicate "Оплата" label -->
<!-- In DELIVERY + PAYMENT two-column section: the payment column has both an h3 "Оплата" header
     AND a separate price line. Check template 02 also for duplicate Оплата label.
     Remove the redundant standalone "Оплата" text if it appears twice in a section. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix templates 01 and 02 (Замовлення оформлено, Підтверджено)</name>
  <files>
    .planning/email/templates/esputnik/01-zamovlennya-oformleno.html
    .planning/email/templates/esputnik/02-pidtverdzheno.html
  </files>
  <action>
Apply all common visual fixes to both templates. For each template:

1. **Logo link**: Wrap the `<img>` logo tag in `<a href="https://miomio.com.ua" target="_blank" style="display:block;line-height:0">` anchor inside the HEADER `<td>`. Keep `background-color:#05125C` on the header TD unchanged.

2. **Color changes** (change `color:#05125C` → `color:#1a1a1a` for these elements only; do NOT touch `background-color:#05125C` on HEADER and FOOTER TDs):
   - h2 heading (e.g. "Дякуємо за замовлення!")
   - h3 headings (e.g. "Товари", "Підсумок", "Доставка", "Оплата")
   - Customer name `<p>` in CUSTOMER INFO
   - Item name `<a>` link in items loop
   - Item price `<p>` in items loop
   - "Всього:" label `<td>` and total amount `<td>` in TOTALS

3. **Price format** — every occurrence of `$!data.get('totalCost') $!data.get('currency')`, `$!item.get('cost') $!data.get('currency')`, and `$!data.get('discount') $!data.get('currency')`:
   Replace pattern: add `#set` before the cell/paragraph, call `intValue()`, replace the old expression.
   Example for totalCost:
   ```
   #set($totalCostInt = $!data.get('totalCost').intValue())
   $totalCostInt грн
   ```
   Use the same pattern for `cost` (items) and `discount` (discount row).

4. **Date format**: Change `$date.format('dd.MM.yyyy', ...)` → `$date.format('dd.MM.yyyy HH:mm', ...)` in all date display expressions.

5. **Remove delivery cost row**: Delete the entire `#if($data.get('shipping')...` Доставка `<tr>` block from the TOTALS section.

6. **Template 01 specific additions**:
   - Move "Наші менеджери зв'яжуться..." text from the MANAGER NOTICE `<tr>` (near bottom, before FOOTER) to directly below the h2 "Дякуємо за замовлення!" in the TITLE section. Place it as a new `<p>` between the h2 and the order number paragraph.
   - Remove the old MANAGER NOTICE `<tr>` from its original position.
   - Update the order info paragraph to include time: `від $date.format('dd.MM.yyyy HH:mm', $date.toDate($data.get('date')))`.

7. **Template 01 duplicate Оплата**: In the DELIVERY + PAYMENT two-column section, the payment column has `<h3>Оплата</h3>` as a column header. Verify there is no second standalone "Оплата" label. If a redundant one exists, remove it.

8. **Template 02**: Apply fixes 1–5 only (no manager notice, no duplicate Оплата check needed per research — template 02 omits delivery/payment section).
  </action>
  <verify>
    Inspect the saved files:
    - `grep -c "intValue" .planning/email/templates/esputnik/01-zamovlennya-oformleno.html` → should be >= 3 (totalCost, item cost, discount)
    - `grep "miomio.com.ua" .planning/email/templates/esputnik/01-zamovlennya-oformleno.html` → shows `href="https://miomio.com.ua"` in header logo anchor
    - `grep "Наші менеджери" .planning/email/templates/esputnik/01-zamovlennya-oformleno.html` → shows text inside TITLE section, NOT in a separate MANAGER NOTICE tr near the bottom
    - `grep "Доставка:" .planning/email/templates/esputnik/01-zamovlennya-oformleno.html` → should return no lines in the TOTALS section (only in DELIVERY info section if present)
    - `grep "HH:mm" .planning/email/templates/esputnik/01-zamovlennya-oformleno.html` → returns date format line
    - `grep -c "intValue" .planning/email/templates/esputnik/02-pidtverdzheno.html` → >= 2
  </verify>
  <done>Templates 01 and 02 have: clickable logo, black text for headings/items/totals, integer prices with грн, date+time, no delivery cost row. Template 01 has manager notice at top under h2.</done>
</task>

<task type="auto">
  <name>Task 2: Fix templates 03 and 04 (Відправлено, Виконано)</name>
  <files>
    .planning/email/templates/esputnik/03-vidpravleno.html
    .planning/email/templates/esputnik/04-vykonano.html
  </files>
  <action>
Apply all common visual fixes to both templates. For each template:

1. **Logo link**: Same as Task 1 — wrap logo `<img>` in `<a href="https://miomio.com.ua" target="_blank" style="display:block;line-height:0">`.

2. **Color changes**: Same set as Task 1 — h2/h3 headings, customer name, item name links, item price, "Всього:" and total amount: change `color:#05125C` → `color:#1a1a1a`. Keep HEADER and FOOTER `background-color:#05125C`.

3. **Price format**: Same `intValue()` pattern as Task 1 for all price expressions including items and totals. Replace `$!data.get('currency')` with hardcoded `грн`.

4. **Date format**: If a date expression exists in template 03 or 04 (`$date.format('dd.MM.yyyy'...`), update to include `HH:mm`.

5. **Remove delivery cost row**: Delete the `#if($data.get('shipping')...` Доставка `<tr>` from TOTALS.

6. **Template 03 specific**: The TRACKING NUMBER block is already present and guarded. The DELIVERY section is single-column. The "Трекінг посилки" h3 currently has `color:#05125C` — change to `color:#1a1a1a`.

7. **Template 04 specific**: Check for duplicate "Оплата" label — if present (column header appears twice), remove the redundant one. Per research, template 04 omits delivery/payment section, so verify this section is already absent; if not, remove it.
  </action>
  <verify>
    - `grep "miomio.com.ua" .planning/email/templates/esputnik/03-vidpravleno.html` → shows `href="https://miomio.com.ua"` in logo anchor
    - `grep "intValue" .planning/email/templates/esputnik/03-vidpravleno.html` → >= 2 occurrences
    - `grep "Доставка:" .planning/email/templates/esputnik/03-vidpravleno.html` → 0 lines in TOTALS section
    - `grep "intValue" .planning/email/templates/esputnik/04-vykonano.html` → >= 2 occurrences
    - `grep "miomio.com.ua" .planning/email/templates/esputnik/04-vykonano.html` → shows anchor in header
  </verify>
  <done>Templates 03 and 04 have: clickable logo, black text, integer prices with грн, no delivery cost row. Template 03 tracking block is preserved with its #if guard unchanged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Update sender display name to "Міо Міо" in eSputnik admin</name>
  <action>The sender display name shown to email recipients ("From:" field) is an account-level setting in eSputnik — it cannot be changed by editing HTML templates. This step must be done by the human in the eSputnik admin panel.</action>
  <instructions>
    1. Log in to your eSputnik account at https://esputnik.com
    2. Go to Account Settings (Налаштування акаунту) → Sender Names (Імена відправників) or similar
    3. Find the sender used by the transactional email workflows (the one currently sending order emails)
    4. Change the display name (From name) to: Міо Міо
    5. Save the change
    6. If the sender name is set per-message or per-workflow rather than globally, update it in each transactional workflow that sends order emails (workflows for INITIALIZED, CONFIRMED, IN_PROGRESS, DELIVERED, CANCELLED, etc.)

    Note: The sender email address itself does not need to change — only the display name shown to recipients.
  </instructions>
  <verify>Send a test email (or trigger a test workflow in eSputnik) and confirm the "From:" field shows "Міо Міо" in the recipient's inbox</verify>
  <done>Outgoing order emails show "Міо Міо" as the sender display name.</done>
  <resume-signal>Type "done" when the sender display name has been updated in eSputnik</resume-signal>
</task>

</tasks>

<verification>
After both auto tasks:
- All 4 templates: `grep -l "intValue" .planning/email/templates/esputnik/0{1,2,3,4}*.html` → 4 files listed
- All 4 templates: `grep -l "miomio.com.ua" .planning/email/templates/esputnik/0{1,2,3,4}*.html` → 4 files listed
- Template 01: manager notice is in TITLE section, not at bottom
- No template has `$!data.get('currency')` remaining (all replaced with `грн`)
- No template has the delivery cost `#if($data.get('shipping')` Доставка row in TOTALS
- eSputnik sender display name updated to "Міо Міо" (confirmed via Task 3 checkpoint)
</verification>

<success_criteria>
Templates 01–04 are updated with all common visual fixes (colors, prices, date+time, logo link, no delivery cost) and template 01 has the manager notice repositioned to the top. Files are ready to paste into eSputnik editor. Sender display name is set to "Міо Міо" in eSputnik account settings.
</success_criteria>

<output>
After completion, create `.planning/phases/05-email-improvements-and-frontend-fixes/05-01-SUMMARY.md`
</output>
