---
phase: 05-email-improvements-and-frontend-fixes
status: in_progress
last_updated: 2026-03-04T17:39:48.965Z
---

<current_state>
All Phase 05 plans complete + all post-execution fixes done this session. All 7 eSputnik email templates uploaded to eSputnik successfully. Bug 1 (order cancel status) fixed. Bug 3 (tracking path) fixed. Bug 2 (keyCRM webhook 200) needs verification. Two manual deployment blockers remain.
</current_state>

<completed_work>

## Phase 05 plans — ALL COMPLETE
- 05-01 through 05-06 all executed and committed

## Fixes completed this session

### keyCRM tracking number (Bug 3) — FIXED
- `context.ttn` → `context.shipping?.tracking_code`
- `KeyCrmWebhookPayload` interface updated with `shipping?: { tracking_code?, tracking_status? }`
- `fulfillOrder` now accepts optional `trackingNumber`, passes to `fulfillmentCreate` via `trackingInfo: { number }`
- `fulfillStatusIds` changed `[12]` → `[10, 12]` — fulfillment fires at ship time (status 10) WITH tracking
- `trackingNumber` extraction hoisted before eSputnik block so it's in scope for `fulfillOrder`
- Files: `app/service/keycrm/keycrm-shopify-sync.service.ts`, `app/shared/config/keycrm.ts`

### Order cancel status on frontend (Bug 1) — FIXED
- `api.order.$orderId.ts`: `fulfillmentStatus: order.cancelledAt ? "СКАСОВАНО" : order.displayFulfillmentStatus`

### OrderStatusBadge — UPDATED
- Added yellow for `IN_PROGRESS` / `PARTIALLY_FULFILLED`
- Added `inProgress` ("Відправлено") and `cancelled` translation keys to uk/en/ru
- Files: `nnshop/src/features/order/ui/OrderStatusBadge.tsx`, `nnshop/messages/{uk,en,ru}.json`

### Email templates 01–07 — EDITED & UPLOADED to eSputnik ✓
- Template 01: removed duplicate `$totalCostInt2 грн` price from Оплата payment column
- Templates 02–07: added order date/time line under subtitle (`externalOrderId` + formatted date)
- Templates 06–07: changed recommendation heading/link color `#05125C` → `#1a1a1a`
- All 7 uploaded via `update-esputnik-templates.ts` — all returned ✓

## Uncommitted changes
- `itali-shop-app`: keycrm-shopify-sync.service.ts, keycrm.ts, api.order.$orderId.ts
- `nnshop`: OrderStatusBadge.tsx, messages/uk.json, messages/en.json, messages/ru.json
- `.planning/email/templates/esputnik/` — 01–07 html files

</completed_work>

<remaining_work>

## Bug 2: keyCRM webhook — verify always returns 200
- File: `app/routes/api.keycrm-webhook.ts`
- The handler already has try/catch around `handleKeyCrmOrderStatusChange` and returns 200 on line 42
- Auth failures return 401 (intentional). JSON parse failures return 400 (intentional, wrapped in try/catch)
- Need to verify: is there any path that can return 500? Likely already OK — confirm and close

## Deployment blockers (manual — need human action in admin panels)
- `PICKUP_ADDRESS_MAP` in `keycrm-shopify-sync.service.ts` needs keyCRM status IDs for 4 READY_FOR_PICKUP stores
- `Відмова від отримання` status ID — add to `esputnikStatusMap` and `cancelStatusIds` in `keycrm.ts`
- eSputnik `priceDropped` workflow not created — manual: Workflows → New → Event: priceDropped → Send message ID 4428255

</remaining_work>

<decisions_made>

- `context.shipping?.tracking_code` is correct path for TTN in keyCRM webhooks (confirmed by keyCRM API docs)
- Fulfillment fires at status 10 (Відправлено) with tracking, status 12 is no-op fallback
- `fulfillmentCreate` supports `trackingInfo: { number }` in Shopify `FulfillmentInput`
- Frontend: override `fulfillmentStatus` to "СКАСОВАНО" when `cancelledAt` is set (Shopify keeps UNFULFILLED after cancel)
- Email rec section: `#05125C` blue → `#1a1a1a` black for body readability; header/footer blue stays
- Order date/time added to templates 02–07 (was only in 01 before)
- Duplicate Оплата price in template 01 removed

</decisions_made>

<blockers>

- PICKUP_ADDRESS_MAP and Відмова від отримання status IDs: blocked on manual keyCRM admin lookup
- eSputnik priceDropped workflow: blocked on manual eSputnik UI setup

</blockers>

<context>
Phase 05 is complete from a code perspective. The session fixed all 3 originally identified bugs (Bug 1: cancel status ✓, Bug 2: needs verify, Bug 3: tracking path ✓), updated all email templates with polish fixes, and uploaded them. The only remaining work is one quick verification (Bug 2) and three manual admin panel steps that require the client.
</context>

<next_action>
1. Commit itali-shop-app: `app/service/keycrm/keycrm-shopify-sync.service.ts`, `app/shared/config/keycrm.ts`, `app/routes/api.order.$orderId.ts`, `.planning/email/templates/esputnik/*.html`
2. Commit nnshop: `src/features/order/ui/OrderStatusBadge.tsx`, `messages/uk.json`, `messages/en.json`, `messages/ru.json`
3. Verify Bug 2: read `app/routes/api.keycrm-webhook.ts` — check for any uncaught 500 paths
4. Manual: populate PICKUP_ADDRESS_MAP (4 store addresses) + Відмова від отримання status ID
5. Manual: create eSputnik priceDropped workflow
</next_action>
