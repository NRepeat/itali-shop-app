---
phase: 05-email-improvements-and-frontend-fixes
status: in_progress
last_updated: 2026-03-04T18:00:00.000Z
---

<current_state>
Renaming `displayFulfillmentStatus` → `fulfillmentStatus` across nnshop is PARTIALLY DONE. 4 files committed, 3 files still need updating.
</current_state>

<completed_work>

## nnshop — partial rename committed (f6a8e22)
- `src/features/order/ui/OrderCard.tsx` — prop renamed, badge usage fixed ✓
- `src/features/order/ui/OrderList.tsx` — prop renamed ✓
- `src/entities/order/api/getCustomerOrders.ts` — mapping key + local type renamed ✓
- `src/entities/order/model/types.ts` — Order interface field renamed ✓

## Still need renaming in nnshop (NOT committed yet)
- `src/features/order/ui/OrderDetails.tsx` lines 128, 132: `order.displayFulfillmentStatus` → `order.fulfillmentStatus`
- `src/entities/order/api/getOrderById.ts` line 44: mapping key `displayFulfillmentStatus` → `fulfillmentStatus`
- `src/entities/order/api/getOrder.ts` line 10: GraphQL query field `displayFulfillmentStatus` — this is a storefront GraphQL field so it STAYS as-is in the query, but the mapped output key should be `fulfillmentStatus`
- `src/entities/order/api/getOrdersByIds.ts` line 15: same as getOrder.ts — query field stays, output key needs check

## Previous session fixes (all committed)
- itali-shop-app: keyCRM tracking path fix, fulfillOrder tracking, cancel status override, email templates
- nnshop: OrderStatusBadge yellow/cancelled, i18n keys
- api.customer.ts: СКАСОВАНО override added

</completed_work>

<remaining_work>

## IMMEDIATE: finish displayFulfillmentStatus rename in nnshop

### `src/features/order/ui/OrderDetails.tsx`
- line 128: `<OrderStatusBadge status={order.displayFulfillmentStatus} />` → `order.fulfillmentStatus`
- line 132: `<OrderTimeline status={order.displayFulfillmentStatus} />` → `order.fulfillmentStatus`
- Also check the Order type used in this file — may need prop rename too

### `src/entities/order/api/getOrderById.ts`
- line 44: `displayFulfillmentStatus: order.fulfillmentStatus` → `fulfillmentStatus: order.fulfillmentStatus`

### `src/entities/order/api/getOrder.ts`
- line 10 is a GraphQL query field (`displayFulfillmentStatus` from storefront API) — keep in query
- Check if there's a mapping step that outputs it — rename that output key to `fulfillmentStatus`

### `src/entities/order/api/getOrdersByIds.ts`
- line 15 is a GraphQL query field — keep in query, check output mapping

## Deployment blockers (manual)
- PICKUP_ADDRESS_MAP in keycrm-shopify-sync.service.ts
- Відмова від отримання status ID in keycrm.ts
- eSputnik priceDropped workflow (message ID 4428255)

</remaining_work>

<decisions_made>
- `displayFulfillmentStatus` is a Shopify storefront GraphQL field name — keep it in GraphQL queries but rename the mapped output key to `fulfillmentStatus` everywhere
- The СКАСОВАНО override lives in itali-shop-app API routes (api.customer.ts, api.order.$orderId.ts) — these return `fulfillmentStatus` already correctly
</decisions_made>

<blockers>
Context exhausted mid-task — resume with /gsd:resume-work
</blockers>

<context>
The rename is half done. The remaining 3-4 file edits are mechanical: in each file, the GraphQL query field `displayFulfillmentStatus` stays in the query string, but the output object key gets renamed to `fulfillmentStatus`. OrderDetails.tsx just needs the two prop references updated.
</context>

<next_action>
1. Read `src/features/order/ui/OrderDetails.tsx` — fix lines 128, 132 (displayFulfillmentStatus → fulfillmentStatus) and the Order type prop
2. Read `src/entities/order/api/getOrderById.ts` — fix output mapping key
3. Read `src/entities/order/api/getOrder.ts` — fix output mapping key (keep query field)
4. Read `src/entities/order/api/getOrdersByIds.ts` — fix output mapping key (keep query field)
5. Commit all nnshop changes
</next_action>
