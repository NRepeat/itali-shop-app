---
phase: 05-email-improvements-and-frontend-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/email/templates/esputnik/05-hotovo-do-samovyvozu.html
  - .planning/email/templates/esputnik/06-tovaru-nemaie-v-nayavnosti.html
  - .planning/email/templates/esputnik/07-skasovano.html
autonomous: true
requirements:
  - ORD-01
must_haves:
  truths:
    - "Templates 05–07 show body/heading text in near-black (#1a1a1a) not navy"
    - "Prices in templates 05–07 display as whole hryvnias with 'грн' label"
    - "Logo in each template header is wrapped in a clickable anchor to miomio.com.ua"
    - "Delivery cost row is absent from TOTALS in templates 05–07"
    - "Template 07 (Скасовано) retains its recommendation block with the double #if guard"
    - "Template 06 (OUT_OF_STOCK) retains its recommendation block with the double #if guard"
  artifacts:
    - path: ".planning/email/templates/esputnik/05-hotovo-do-samovyvozu.html"
      provides: "READY_FOR_PICKUP email template"
      contains: "intValue"
    - path: ".planning/email/templates/esputnik/06-tovaru-nemaie-v-nayavnosti.html"
      provides: "OUT_OF_STOCK email template"
      contains: "recommendedItems"
    - path: ".planning/email/templates/esputnik/07-skasovano.html"
      provides: "CANCELLED email template"
      contains: "recommendedItems"
  key_links:
    - from: "recommendation block guard"
      to: "eSputnik engine recommendedItems variable"
      via: "#if($data.get('recommendedItems')) #if($data.get('recommendedItems').size() gt 0)"
      pattern: "recommendedItems"
---

<objective>
Apply visual and formatting fixes to email templates 05–07 (Готово до самовивозу, Товар недоступний, Скасовано).

Purpose: Same common fix set as plan 01 (colors, prices, date, logo link, no delivery cost), applied to the remaining three templates. The recommendation blocks in templates 06 and 07 must be preserved exactly as-is.
Output: Three updated HTML files ready to paste into eSputnik editor.
</objective>

<execution_context>
@/Users/mnmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mnmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-email-improvements-and-frontend-fixes/05-CONTEXT.md
@.planning/phases/05-email-improvements-and-frontend-fixes/05-RESEARCH.md

<interfaces>
<!-- Same Velocity fix patterns as plan 01 — repeated here for executor convenience -->

<!-- Price format: -->
#set($cost = $!data.get('totalCost'))
#set($intCost = $cost.intValue())
$intCost грн
<!-- Apply same #set + intValue pattern for item cost and discount if present -->

<!-- Date format: -->
$date.format('dd.MM.yyyy HH:mm', $date.toDate($data.get('date')))

<!-- Logo anchor: -->
<a href="https://miomio.com.ua" target="_blank" style="display:block;line-height:0">
  <img src="..." alt="Mio Mio" height="55" style="display:block;margin:0 auto;max-width:180px">
</a>

<!-- Color: body text #05125C → #1a1a1a (NOT backgrounds) -->

<!-- Recommendation block in templates 06/07 — DO NOT TOUCH, preserve exactly: -->
#if($data.get('recommendedItems'))
#if($data.get('recommendedItems').size() gt 0)
  ... recommendation rendering loop ...
#end
#end
<!-- The engine populates recommendedItems automatically via externalItemId on order items -->
<!-- No code changes needed — eSputnik admin must configure the recommendation engine separately -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix template 05 (Готово до самовивозу)</name>
  <files>
    .planning/email/templates/esputnik/05-hotovo-do-samovyvozu.html
  </files>
  <action>
Apply all common visual fixes to template 05:

1. **Logo link**: Wrap the logo `<img>` in `<a href="https://miomio.com.ua" target="_blank" style="display:block;line-height:0">`. Keep `background-color:#05125C` on the header TD.

2. **Color changes** (`color:#05125C` → `color:#1a1a1a` for these elements only):
   - h2 heading ("Замовлення готове до видачі")
   - h3 headings (if any — "Товари", "Підсумок", "Адреса самовивозу")
   - Customer name `<p>` in customer info section
   - Item name `<a>` links in items loop
   - Item price `<p>` in items loop
   - "Всього:" and total amount cells

3. **Price format**: Apply `#set($...Int = $!data.get('...').intValue())` pattern to all price expressions. Replace `$!data.get('currency')` with hardcoded `грн`.

4. **Date format**: Update `$date.format('dd.MM.yyyy', ...)` to `$date.format('dd.MM.yyyy HH:mm', ...)`.

5. **Remove delivery cost row**: Delete the `#if($data.get('shipping')...` Доставка `<tr>` from TOTALS section.

6. **Pickup address section**: The pickup address display uses `$!data.get('pickupAddress')` — this is correct and must be preserved. Only change its `color:#05125C` text to `color:#1a1a1a`.
  </action>
  <verify>
    - `grep "miomio.com.ua" .planning/email/templates/esputnik/05-hotovo-do-samovyvozu.html` → shows href in logo anchor
    - `grep "intValue" .planning/email/templates/esputnik/05-hotovo-do-samovyvozu.html` → >= 1
    - `grep "pickupAddress" .planning/email/templates/esputnik/05-hotovo-do-samovyvozu.html` → still present (not removed)
    - `grep "Доставка:" .planning/email/templates/esputnik/05-hotovo-do-samovyvozu.html` → 0 lines in TOTALS
  </verify>
  <done>Template 05 has clickable logo, black text, integer hryvnia prices, no delivery cost row; pickupAddress section preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Fix templates 06 and 07 (Товар недоступний, Скасовано)</name>
  <files>
    .planning/email/templates/esputnik/06-tovaru-nemaie-v-nayavnosti.html
    .planning/email/templates/esputnik/07-skasovano.html
  </files>
  <action>
Apply all common visual fixes to both templates. These templates already contain recommendation blocks — they must remain untouched.

**For each template (06 and 07):**

1. **Logo link**: Wrap logo `<img>` in `<a href="https://miomio.com.ua" target="_blank" style="display:block;line-height:0">`.

2. **Color changes** (`color:#05125C` → `color:#1a1a1a` for body text elements):
   - h2 heading (e.g. "Товар недоступний", "Замовлення скасовано")
   - h3 headings
   - Customer name `<p>`
   - Item name `<a>` links in items loop
   - Item price `<p>`
   - "Всього:" and total amount cells (template 07 has totals; template 06 omits totals per research)

3. **Price format**: Apply `intValue()` pattern to all price expressions. Replace `$!data.get('currency')` with `грн`.

4. **Date format**: Update any date format expression to include `HH:mm`.

5. **Remove delivery cost row**: Delete the shipping `#if` Доставка `<tr>` from TOTALS if present.

6. **CRITICAL — Preserve recommendation blocks**: The `#if($data.get('recommendedItems')) #if($data.get('recommendedItems').size() gt 0)` blocks in both templates MUST NOT be modified. Only apply color/price fixes to elements outside these blocks. The recommendation section renders items from the eSputnik engine — no code changes needed.

7. **Template 07 totals**: Per research, template 07 includes totals (order existed before cancellation). Apply price fixes to those totals.

8. **Template 06 totals**: Per research, template 06 omits totals. If a totals section exists, leave it; if absent, do not add it.
  </action>
  <verify>
    - `grep "miomio.com.ua" .planning/email/templates/esputnik/06-tovaru-nemaie-v-nayavnosti.html` → shows anchor
    - `grep "recommendedItems" .planning/email/templates/esputnik/06-tovaru-nemaie-v-nayavnosti.html` → present (not removed)
    - `grep "miomio.com.ua" .planning/email/templates/esputnik/07-skasovano.html` → shows anchor
    - `grep "recommendedItems" .planning/email/templates/esputnik/07-skasovano.html` → present (not removed)
    - `grep "intValue" .planning/email/templates/esputnik/07-skasovano.html` → >= 1
  </verify>
  <done>Templates 06 and 07 have all visual fixes applied; recommendation blocks are intact and unmodified.</done>
</task>

</tasks>

<verification>
After both tasks:
- All three templates have `miomio.com.ua` anchor wrapping logo
- Templates 06 and 07 still contain `recommendedItems` variable references
- No template has `$!data.get('currency')` remaining (all → `грн`)
- No template has the delivery cost Доставка row in TOTALS
</verification>

<success_criteria>
Templates 05, 06, and 07 are updated with all common visual fixes. Recommendation blocks in templates 06/07 are preserved exactly. Files are ready to paste into eSputnik editor.
</success_criteria>

<output>
After completion, create `.planning/phases/05-email-improvements-and-frontend-fixes/05-02-SUMMARY.md`
</output>
