---
phase: 05-email-improvements-and-frontend-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/mnmac/Development/nnshop/src/features/product/quick-buy/ui/QuickBuyModal.tsx
  - /Users/mnmac/Development/nnshop/src/widgets/footer/ui/Footer.tsx
  - /Users/mnmac/Development/nnshop/src/features/checkout/receipt/ui/DeliveryInfo.tsx
autonomous: false
requirements:
  - ORD-01
must_haves:
  truths:
    - "Quick order modal default phone value is '+380' (not '+38')"
    - "Footer displays a Viber icon link next to the phone numbers"
    - "Viber deep link opens Viber with the correct phone number format"
    - "Cart opens immediately when product is added (existing behavior confirmed or fixed)"
    - "Order status badge already handles Відмова від отримання as red (confirmed, no code change)"
    - "Checkout receipt sidebar does not show delivery/shipping section or delivery cost"
  artifacts:
    - path: "/Users/mnmac/Development/nnshop/src/features/product/quick-buy/ui/QuickBuyModal.tsx"
      provides: "Quick order form with correct phone default"
      contains: "'+380'"
    - path: "/Users/mnmac/Development/nnshop/src/widgets/footer/ui/Footer.tsx"
      provides: "Footer with Viber contact icon"
      contains: "viber://"
    - path: "/Users/mnmac/Development/nnshop/src/features/checkout/receipt/ui/DeliveryInfo.tsx"
      provides: "Checkout receipt delivery section — removed or emptied"
  key_links:
    - from: "QuickBuyModal useForm defaultValues"
      to: "phone field initial value"
      via: "phone: '+380'"
      pattern: "phone.*\\+380"
    - from: "Footer ViberIcon component"
      to: "viber:// deep link"
      via: "href=\"viber://chat?number=...\""
      pattern: "viber://chat"
    - from: "checkout receipt sidebar"
      to: "DeliveryInfoSection component"
      via: "removed from parent layout — no delivery section rendered"
      pattern: "DeliveryInfo"
---

<objective>
Three targeted frontend fixes in the nnshop storefront: change quick order default phone code to +380, add a Viber icon/link to the Footer contact section, and remove the delivery/shipping section from the checkout receipt sidebar. Also verify (no code change needed) that cart drawer and order status badge already work correctly.

Purpose: Quick order form starts with the correct Ukrainian code. Footer provides Viber as a contact channel. Checkout shows no delivery cost or delivery section per user decision. Status badges already handle the new Відмова від отримання status.
Output: Three updated TSX files in nnshop.
</objective>

<execution_context>
@/Users/mnmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mnmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-email-improvements-and-frontend-fixes/05-CONTEXT.md
@.planning/phases/05-email-improvements-and-frontend-fixes/05-RESEARCH.md

<interfaces>
<!-- From QuickBuyModal.tsx — two places where phone default appears: -->
// 1. useForm defaultValues (line ~88-92):
defaultValues: {
  name: '',
  phone: '+38',  // Change to '+380'
},

// 2. form.reset inside useEffect (line ~106-108):
form.reset({ name: '', phone: '+38' });  // Change to '+380'

<!-- From Footer.tsx — current contact section (lines ~187-196): -->
<div className="flex items-start gap-2 text-white/60 text-sm">
  <Phone className="w-4 h-4 mt-0.5 shrink-0" />
  <div className="flex flex-col gap-0.5">
    <a href={`tel:${phone1.replace(/-/g, '')}`} className="hover:text-white transition-colors">
      {phone1}
    </a>
    <a href={`tel:${phone2.replace(/-/g, '')}`} className="hover:text-white transition-colors">
      {phone2}
    </a>
  </div>
</div>
<!-- Add ViberIcon inline SVG and viber:// link after the phone block -->

<!-- Footer already has inline brand icons (FacebookIcon, InstagramIcon) — follow same pattern: -->
const ViberIcon = () => (
  <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
    <path d="M11.4 0C5.1 0 0 5.1 0 11.4c0 3.2 1.3 6 3.4 8.1L1.2 24l4.7-2.1C7.7 23.3 9.5 24 11.4 24 17.7 24 22.8 18.9 22.8 12.6 22.8 5.7 17.7.6 11.4 0z"/>
  </svg>
);
<!-- phone1 = '097-217-92-92' from translations, digits only = '0972179292' -->
<!-- Viber deep link: viber://chat?number=%2B380972179292 -->

<!-- OrderStatusBadge already handles Відмова від отримання as red — no change needed: -->
const isRed = ['CANCELLED', 'СКАСОВАНО', 'ВІДМОВА ВІД ОТРИМАННЯ', 'ON_HOLD', 'ОТМЕНЕН'].includes(upper);

<!-- From DeliveryInfo.tsx — full component (async server component, ~71 lines):
  The component renders either a tooltip card with delivery address/Nova Poshta branch,
  or a PlaceHolder if no delivery info is set.
  Decision: Remove delivery/shipping section from checkout entirely — no delivery cost shown or calculated.
  The simplest removal is to return null from the component (making it a no-op),
  OR find where DeliveryInfoSection is rendered in the checkout receipt layout and remove that usage.
  Prefer removing the JSX usage from the parent receipt layout so the component file itself is
  left clean (easier to restore later if needed).
  Search for DeliveryInfoSection usage: grep -rn "DeliveryInfoSection\|DeliveryInfo" /Users/mnmac/Development/nnshop/src/features/checkout/receipt/
  Then remove the import and JSX element from the parent.
  If the parent layout is not findable quickly, returning null from DeliveryInfo.tsx directly is acceptable.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix quick order phone default and add Viber to Footer</name>
  <files>
    /Users/mnmac/Development/nnshop/src/features/product/quick-buy/ui/QuickBuyModal.tsx
    /Users/mnmac/Development/nnshop/src/widgets/footer/ui/Footer.tsx
  </files>
  <action>
**In `QuickBuyModal.tsx`:**

Change the phone default in TWO places:
1. `useForm` `defaultValues`: `phone: '+38'` → `phone: '+380'`
2. `form.reset(...)` inside `useEffect` on `open` change: `phone: '+38'` → `phone: '+380'`

**In `Footer.tsx`:**

1. Add a `ViberIcon` inline SVG component at the top of the file, alongside `FacebookIcon` and `InstagramIcon`:

```tsx
const ViberIcon = () => (
  <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
    <path d="M11.4 0C5.1 0 0 5.1 0 11.4c0 3.2 1.3 6 3.4 8.1L1.2 24l4.7-2.1C7.7 23.3 9.5 24 11.4 24 17.7 24 22.8 18.9 22.8 12.6 22.8 5.7 17.7.6 11.4 0z"/>
  </svg>
);
```

2. In the contact section (Column 4 — Working hours & contacts), add a Viber link row after the phone `<div>`:

```tsx
<div className="flex items-center gap-2 text-white/60 text-sm">
  <ViberIcon />
  <a
    href="viber://chat?number=%2B380972179292"
    className="hover:text-white transition-colors"
  >
    Viber: {phone1}
  </a>
</div>
```

Place this new div after the phone `<div>` (which contains phone1 and phone2 tel links) and before the email `<div>`.

The phone1 translation value is '097-217-92-92'. The Viber deep link uses the E.164 format without dashes: `%2B380972179292` (URL-encoded `+380972179292`). The displayed label shows `Viber: {phone1}` to make it clear.
  </action>
  <verify>
    - `grep "+380'" /Users/mnmac/Development/nnshop/src/features/product/quick-buy/ui/QuickBuyModal.tsx` → 2 occurrences (defaultValues and form.reset)
    - `grep "+38'" /Users/mnmac/Development/nnshop/src/features/product/quick-buy/ui/QuickBuyModal.tsx` → 0 occurrences (old value removed)
    - `grep "viber://" /Users/mnmac/Development/nnshop/src/widgets/footer/ui/Footer.tsx` → present
    - `grep "ViberIcon" /Users/mnmac/Development/nnshop/src/widgets/footer/ui/Footer.tsx` → present (definition and usage)
  </verify>
  <done>Quick order modal starts phone input at +380. Footer shows Viber icon with a viber:// deep link to the correct phone number.</done>
</task>

<task type="auto">
  <name>Task 2: Remove delivery section from checkout receipt sidebar</name>
  <files>
    /Users/mnmac/Development/nnshop/src/features/checkout/receipt/ui/DeliveryInfo.tsx
  </files>
  <action>
The context decision is: "Remove delivery/shipping section from checkout entirely (no delivery cost shown or calculated)."

`DeliveryInfo.tsx` is an async server component that renders a delivery address card (Nova Poshta branch or address fields) in the checkout receipt sidebar. It must be removed from the checkout UI.

**Step 1 — Find the parent that renders DeliveryInfoSection:**
```bash
grep -rn "DeliveryInfoSection\|DeliveryInfo" /Users/mnmac/Development/nnshop/src/features/checkout/receipt/ --include="*.tsx"
```

**Step 2a — If a parent file is found:**
Remove the `import` statement for `DeliveryInfoSection` and remove the `<DeliveryInfoSection ... />` JSX element from that file. Do not delete `DeliveryInfo.tsx` itself — just remove its usage.

**Step 2b — If no parent is found in that directory (component imported elsewhere):**
Widen the search:
```bash
grep -rn "DeliveryInfoSection\|from.*DeliveryInfo" /Users/mnmac/Development/nnshop/src/ --include="*.tsx"
```
Then remove the import and JSX usage from whichever file renders it.

**Step 2c — Fallback if the import location cannot be determined quickly:**
In `DeliveryInfo.tsx` itself, replace the entire return block with `return null;` so the component renders nothing. Keep the function signature intact — do not delete the file.

The goal is that visiting the checkout receipt page shows no delivery address card, no delivery cost, and no placeholder for delivery info.
  </action>
  <verify>
    - `grep -rn "DeliveryInfoSection" /Users/mnmac/Development/nnshop/src/ --include="*.tsx"` → either 0 usages, or only in DeliveryInfo.tsx itself (not rendered anywhere)
    - OR if fallback was used: `grep "return null" /Users/mnmac/Development/nnshop/src/features/checkout/receipt/ui/DeliveryInfo.tsx` → present
    - `cd /Users/mnmac/Development/nnshop && npx tsc --noEmit 2>&1 | grep DeliveryInfo` → no type errors
  </verify>
  <done>Checkout receipt sidebar no longer renders any delivery address or delivery cost section. No TypeScript errors introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all frontend changes and confirm existing behaviors</name>
  <action>Human verification of all frontend fixes applied in Tasks 1–2 plus confirmation of already-working cart drawer and status badge behaviors.</action>
  <what-built>
    Quick order phone default changed to +380 (QuickBuyModal.tsx). Viber icon and link added to Footer.tsx. Delivery section removed from checkout receipt sidebar (DeliveryInfo.tsx). Additionally confirming existing behaviors: cart drawer opens on add-to-cart, OrderStatusBadge correctly colors "Відмова від отримання" as red.
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd /Users/mnmac/Development/nnshop && npm run dev`
    2. Open any product page → click "Швидке замовлення" → confirm phone field starts with "+380" (not "+38")
    3. Add any item to cart → confirm cart sidebar/drawer opens immediately
    4. Open Footer → confirm Viber icon appears next to phone numbers, and clicking it opens Viber (on mobile) or shows a viber:// link (on desktop)
    5. Navigate to the checkout receipt page → confirm NO delivery address card or delivery cost section appears in the right sidebar
    6. Navigate to account page → find any order with "Відмова від отримання" status (or check the badge component directly) → confirm it shows as red
  </how-to-verify>
  <verify>Human confirms all points above</verify>
  <done>All visual and behavioral checks pass. Checkout receipt shows no delivery section.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Code checks:
- `grep "+380'" /Users/mnmac/Development/nnshop/src/features/product/quick-buy/ui/QuickBuyModal.tsx` → 2 matches
- `grep "viber://" /Users/mnmac/Development/nnshop/src/widgets/footer/ui/Footer.tsx` → 1 match
- DeliveryInfoSection not rendered in checkout receipt (either no usages or DeliveryInfo.tsx returns null)
</verification>

<success_criteria>
Quick order phone default is +380. Footer has Viber icon with deep link. Checkout receipt sidebar shows no delivery section. Cart-open and status badge behaviors are verified as working.
</success_criteria>

<output>
After completion, create `.planning/phases/05-email-improvements-and-frontend-fixes/05-05-SUMMARY.md`
</output>
