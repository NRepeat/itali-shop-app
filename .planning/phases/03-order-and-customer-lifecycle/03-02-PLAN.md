---
phase: 03-order-and-customer-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/service/shopify/customers/sync.ts
autonomous: true
requirements:
  - CUST-01
must_haves:
  truths:
    - "A customer syncer implementation exists that extends BaseSyncer."
    - "The customer syncer handles customer updates from Shopify."
  artifacts:
    - path: "app/service/shopify/customers/sync.ts"
      provides: "CustomerSyncer class that extends BaseSyncer"
      min_lines: 20
      contains: "class CustomerSyncer extends BaseSyncer"
---

<objective>
Implement the `CustomerSyncer` class to handle customer updates from Shopify.

Purpose: This plan addresses the core of the customer synchronization by creating the main syncer for customers.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/service/sync/base.syncer.ts
</context>

<tasks>
<task type="auto">
  <name>Implement Customer Syncer</name>
  <files>
    - app/service/shopify/customers/sync.ts
  </files>
  <action>
    1. **Create `app/service/shopify/customers/sync.ts`**:
       - Create a `CustomerSyncer` class that extends `BaseSyncer`.
       - Implement the abstract methods from `BaseSyncer`.
       - The `process` method should handle the customer update payload from Shopify.
       - The `isStale` method should compare the `updated_at` timestamp from the webhook payload with the one in the local database.
  </action>
  <verify>
    <automated>
      test -f app/service/shopify/customers/sync.ts &&
      grep -q "class CustomerSyncer extends BaseSyncer" app/service/shopify/customers/sync.ts
    </automated>
  </verify>
</task>
</tasks>

<verification>
After the task in this plan is complete:
- The `CustomerSyncer` class is implemented and extends `BaseSyncer`.
</verification>

<success_criteria>
- The `CustomerSyncer` can process customer update webhooks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-order-and-customer-lifecycle/03-02-SUMMARY.md`
</output>
