---
phase: 03-order-and-customer-lifecycle
plan: 01
type: execute
wave: 1
depends_on:
  - 02-03
files_modified:
  - app/service/shopify/orders/sync.ts
autonomous: true
requirements:
  - ORD-01
  - ORD-02
must_haves:
  truths:
    - "An order syncer implementation exists that extends BaseSyncer."
    - "The order syncer handles order updates from Shopify."
  artifacts:
    - path: "app/service/shopify/orders/sync.ts"
      provides: "OrderSyncer class that extends BaseSyncer"
      min_lines: 20
      contains: "class OrderSyncer extends BaseSyncer"
---

<objective>
Implement the `OrderSyncer` class to handle order updates from Shopify.

Purpose: This plan addresses the core of the order synchronization by creating the main syncer for orders.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/service/sync/base.syncer.ts
</context>

<tasks>
<task type="auto">
  <name>Implement Order Syncer</name>
  <files>
    - app/service/shopify/orders/sync.ts
  </files>
  <action>
    1. **Create `app/service/shopify/orders/sync.ts`**:
       - Create a `OrderSyncer` class that extends `BaseSyncer`.
       - Implement the abstract methods from `BaseSyncer`.
       - The `process` method should handle the order update payload from Shopify.
       - The `isStale` method should compare the `updated_at` timestamp from the webhook payload with the one in the local database.
  </action>
  <verify>
    <automated>
      test -f app/service/shopify/orders/sync.ts &&
      grep -q "class OrderSyncer extends BaseSyncer" app/service/shopify/orders/sync.ts
    </automated>
  </verify>
</task>
</tasks>

<verification>
After the task in this plan is complete:
- The `OrderSyncer` class is implemented and extends `BaseSyncer`.
</verification>

<success_criteria>
- The `OrderSyncer` can process order update webhooks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-order-and-customer-lifecycle/03-01-SUMMARY.md`
</output>
