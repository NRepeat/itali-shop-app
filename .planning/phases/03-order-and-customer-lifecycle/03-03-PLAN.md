---
phase: 03-order-and-customer-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - app/service/mapping/orders.ts
autonomous: true
requirements:
  - ORD-01
must_haves:
  truths:
    - "A mechanism exists to map orders between systems."
    - "A mechanism exists to re-map or fix orphan orders."
  artifacts:
    - path: "app/service/mapping/orders.ts"
      provides: "Logic for mapping and re-mapping orders"
      min_lines: 20
      contains: "mapOrder"
---

<objective>
Implement a mechanism to map orders between systems and to re-map or fix orphan orders.

Purpose: This plan ensures that orders are correctly mapped between Shopify and the internal system, and that any mapping issues can be resolved.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Implement Order Mapping</name>
  <files>
    - app/service/mapping/orders.ts
  </files>
  <action>
    1. **Create `app/service/mapping/orders.ts`**:
       - Create a `mapOrder` function that takes a Shopify order and maps it to the internal order format.
       - Create a `remapOrder` function that can be used to fix orphan orders.
  </action>
  <verify>
    <automated>
      test -f app/service/mapping/orders.ts &&
      grep -q "mapOrder" app/service/mapping/orders.ts
    </automated>
  </verify>
</task>
</tasks>

<verification>
After the task in this plan is complete:
- The order mapping logic is implemented.
</verification>

<success_criteria>
- A mechanism exists to map orders between systems.
- A mechanism exists to re-map or fix orphan orders.
</success_criteria>

<output>
After completion, create `.planning/phases/03-order-and-customer-lifecycle/03-03-SUMMARY.md`
</output>
