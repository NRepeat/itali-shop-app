---
phase: 02-product-and-handle-reconciliation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - app/service/shopify/products/reconciliation.ts
autonomous: true
requirements:
  - SYNC-01
must_haves:
  truths:
    - "A background job can identify and fix drift between Shopify and local data."
  artifacts:
    - path: "app/service/shopify/products/reconciliation.ts"
      provides: "Logic for reconciling product data"
      min_lines: 20
      contains: "reconcileProducts"
---

<objective>
Implement a background reconciliation job that can identify and fix drift between Shopify and local product data.

Purpose: This plan ensures that the local product data is kept in sync with Shopify, even if some webhook events are missed.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/service/shopify/products/sync.ts
</context>

<tasks>
<task type="auto">
  <name>Implement Product Reconciliation Job</name>
  <files>
    - app/service/shopify/products/reconciliation.ts
  </files>
  <action>
    1. **Create `app/service/shopify/products/reconciliation.ts`**:
       - Create a `reconcileProducts` function that fetches all products from Shopify and compares them with the local data.
       - If any drift is detected, the function should update the local data to match Shopify.
       - This function will be called by a background job.
  </action>
  <verify>
    <automated>
      test -f app/service/shopify/products/reconciliation.ts &&
      grep -q "reconcileProducts" app/service/shopify/products/reconciliation.ts
    </automated>
  </verify>
</task>
</tasks>

<verification>
After the task in this plan is complete:
- The product reconciliation job is implemented.
</verification>

<success_criteria>
- A background reconciliation job can identify and fix drift between Shopify and local data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-and-handle-reconciliation/02-03-SUMMARY.md`
</output>
