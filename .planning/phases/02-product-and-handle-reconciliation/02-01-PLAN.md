---
phase: 02-product-and-handle-reconciliation
plan: 01
type: execute
wave: 1
depends_on:
  - 01-03
files_modified:
  - app/service/shopify/products/sync.ts
  - app/service/shopify/products/utils.ts
autonomous: true
requirements:
  - PROD-01
  - PROD-02
must_haves:
  truths:
    - "A product syncer implementation exists that extends BaseSyncer."
    - "The product syncer handles product updates from Shopify."
    - "Product handles are sanitized and stripped of brand names."
  artifacts:
    - path: "app/service/shopify/products/sync.ts"
      provides: "ProductSyncer class that extends BaseSyncer"
      min_lines: 20
      contains: "class ProductSyncer extends BaseSyncer"
    - path: "app/service/shopify/products/utils.ts"
      provides: "Utility functions for handle sanitization"
      min_lines: 10
      exports: ["sanitizeHandle", "stripBrandFromHandle"]
---

<objective>
Implement the `ProductSyncer` class to handle product updates from Shopify, and create utility functions for sanitizing and stripping brand names from product handles.

Purpose: This plan addresses the core of the product synchronization and handle reconciliation by creating the main syncer and the handle utility functions.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sync-foundation-architecture/01-CONTEXT.md
@app/service/sync/base.syncer.ts
</context>

<tasks>
<task type="auto">
  <name>Implement Product Syncer</name>
  <files>
    - app/service/shopify/products/sync.ts
  </files>
  <action>
    1. **Create `app/service/shopify/products/sync.ts`**:
       - Create a `ProductSyncer` class that extends `BaseSyncer`.
       - Implement the abstract methods from `BaseSyncer`.
       - The `process` method should handle the product update payload from Shopify.
       - The `isStale` method should compare the `updated_at` timestamp from the webhook payload with the one in the local database.
  </action>
  <verify>
    <automated>
      test -f app/service/shopify/products/sync.ts &&
      grep -q "class ProductSyncer extends BaseSyncer" app/service/shopify/products/sync.ts
    </automated>
  </verify>
</task>
<task type="auto">
  <name>Implement Handle Utilities</name>
  <files>
    - app/service/shopify/products/utils.ts
  </files>
  <action>
    1. **Create `app/service/shopify/products/utils.ts`**:
       - Create a `sanitizeHandle` function that removes special characters and converts the handle to lowercase.
       - Create a `stripBrandFromHandle` function that removes brand names from the handle.
  </action>
  <verify>
    <automated>
      test -f app/service/shopify/products/utils.ts &&
      grep -q "export function sanitizeHandle" app/service/shopify/products/utils.ts &&
      grep -q "export function stripBrandFromHandle" app/service/shopify/products/utils.ts
    </automated>
  </verify>
</task>
</tasks>

<verification>
After both tasks in this plan are complete:
- The `ProductSyncer` class is implemented and extends `BaseSyncer`.
- The handle utility functions are implemented and exported.
</verification>

<success_criteria>
- The `ProductSyncer` can process product update webhooks.
- Product handles are sanitized and stripped of brand names.
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-and-handle-reconciliation/02-01-SUMMARY.md`
</output>
