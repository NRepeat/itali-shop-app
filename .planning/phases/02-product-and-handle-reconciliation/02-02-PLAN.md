---
phase: 02-product-and-handle-reconciliation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/service/shopify/products/sync.ts
autonomous: true
requirements:
  - PROD-03
must_haves:
  truths:
    - "The ProductSyncer can detect and resolve handle collisions."
    - "A SKU or ID is used as a suffix to resolve collisions."
  artifacts:
    - path: "app/service/shopify/products/sync.ts"
      provides: "Logic for resolving handle collisions"
      min_lines: 10
      contains: "resolveHandleCollision"
---

<objective>
Implement logic in the `ProductSyncer` to detect and resolve handle collisions by appending a SKU or ID as a suffix.

Purpose: This plan ensures that product handles are unique, which is essential for clean URLs and preventing data corruption.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/service/shopify/products/sync.ts
</context>

<tasks>
<task type="auto">
  <name>Implement Handle Collision Resolution</name>
  <files>
    - app/service/shopify/products/sync.ts
  </files>
  <action>
    1. **Modify `app/service/shopify/products/sync.ts`**:
       - Add a `resolveHandleCollision` method to the `ProductSyncer` class.
       - This method should check if a handle already exists in the database.
       - If the handle exists, it should append the product's SKU or ID to the handle to make it unique.
       - The `process` method should be updated to use this new method.
  </action>
  <verify>
    <automated>
      grep -q "resolveHandleCollision" app/service/shopify/products/sync.ts
    </automated>
  </verify>
</task>
</tasks>

<verification>
After the task in this plan is complete:
- The `ProductSyncer` can resolve handle collisions.
</verification>

<success_criteria>
- Handle collisions are automatically resolved using a SKU or ID suffix.
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-and-handle-reconciliation/02-02-SUMMARY.md`
</output>
